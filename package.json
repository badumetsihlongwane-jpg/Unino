{
  "name": "unino",
  "version": "1.0.0",
  "private": true,
  "description": "Unino mobile wrapper and web packaging scripts",
  "scripts": {
    "build:web": "node scripts/prepare-web.mjs",
    "cap:sync": "npm run build:web && npx cap sync android",
    "cap:open:android": "npx cap open android",
    "apk:debug": "npm run build:web && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.9.3"
  }
}
